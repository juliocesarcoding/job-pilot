{
  "name": "@job-pilot/worker",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "tsx watch src/index.ts",
    "build": "tsc -p tsconfig.json",
    "start": "node dist/index.js",
    "lint": "eslint \"src/**/*.ts\"",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "@job-pilot/shared": "workspace:*",
    "bullmq": "^5.0.0",
    "ioredis": "^5.0.0",
    "zod": "^3.22.0"
  },
  "devDependencies": {
    "tsx": "^4.19.3"
  }
}
