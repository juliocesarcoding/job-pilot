# Data model

The initial data model is intentionally minimal. Prisma currently contains a single health-check model to confirm that the database package is wired correctly. More domain entities will be introduced in later phases.
